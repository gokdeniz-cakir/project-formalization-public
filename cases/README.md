# Public cases

Each case directory is self-contained: the public advisory context, preserved
evidence allowed for release, an explicit claim boundary, and any portable
model or replay are kept together. The repository does not vendor upstream
source trees; acquisition is described per case so pins stay reviewable.
